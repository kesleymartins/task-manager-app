<%= form_with(model: Task.new, class: "flex fd-row jc-center") do |f| %>
  <%= f.text_field :title, placeholder: 'Task name', class: "field" %>

  <%= f.submit 'Create Task', class: 'button btn-create ml-1' %>
<% end %>
